<script>
  function initialize() {
    var myLatlng = new google.maps.LatLng(<%= "#{@report.latitude}, #{@report.longtitude}" %>);
    var myOptions = {
        zoom: 15,
        center: myLatlng,
        mapTypeId: google.maps.MapTypeId.ROADMAP
    }
    var map = new google.maps.Map(document.getElementById("map_canvas"), myOptions);
    var beachMarker = new google.maps.Marker({
        position: myLatlng,
        <%= "icon:'/images/green-marker.png'," if @report.fixed? %>
        map: map
    });
  }

  function loadScript() {
    var script = document.createElement("script");
    script.type = "text/javascript";
    script.src = "http://maps.google.com/maps/api/js?sensor=false&callback=initialize";
    document.body.appendChild(script);
  }

  window.onload = loadScript;
</script>
<div class="grid_8">
  <div id="map_canvas"></div>
  <div id="disqus_thread"></div>
</div>
<div class="grid_4">
  <h6><%= link_to @report.user.name, @report.user.link %></h6>
  <h3><%=@report.title %></h3>
  <%= render "meta_box" %>
  <%=@report.description %><br /><br />
  <ul class="images">
    <%= content_tag(:li, link_to(image_tag(@report.photo1.url(:medium)), @report.photo1.url)) if @report.photo1? %>
    <%= content_tag(:li, link_to(image_tag(@report.photo2.url(:medium)), @report.photo2.url)) if @report.photo2? %>
    <%= content_tag(:li, link_to(image_tag(@report.photo3.url(:medium)), @report.photo3.url)) if @report.photo3? %>
    <%= content_tag(:li, link_to(image_tag(@report.photo4.url(:medium)), @report.photo4.url)) if @report.photo4? %>
    <%= content_tag(:li, link_to(image_tag(@report.photo5.url(:medium)), @report.photo5.url)) if @report.photo5? %>
  </ul>
</div>

<div class="clear"></div>
<script type="text/javascript">
    /* * * CONFIGURATION VARIABLES: EDIT BEFORE PASTING INTO YOUR WEBPAGE * * */
    var disqus_shortname = 'fixmystreet'; // required: replace example with your forum shortname
    var disqus_developer = 0;

    // The following are highly recommended additional parameters. Remove the slashes in front to use.
    //var disqus_identifier = '<%= @report.id %>';
    //var disqus_url = 'http://fixmystreet.kg<%= report_path @report %>';
    window.disqus_no_style = true;
    $.getScript("http://disqus.com/forums/fixmystreet/embed.js")

</script>
<noscript>Please enable JavaScript to view the <a href="http://disqus.com/?ref_noscript">comments powered by Disqus.</a></noscript>
